# Pose model

`pose_landmarker_lite.task` is Google's MediaPipe Pose Landmarker Lite model bundle, downloaded from the official MediaPipe model storage. It provides the 33 normalized landmarks used for the live overlay. Model-derived coordinates and NeuroFlex scores remain investigational until benchmarked against the project validation datasets.
